// XP BAR DEMO ANIMATION (UI only for now)
window.addEventListener("load", () => {
    const fill = document.getElementById("tanuki-xp-fill");

    // TEMP: animate to 40% so you can SEE it working
    setTimeout(() => {
        fill.style.width = "40%";
    }, 300);
});
